import React from 'react';
import Timetable from '../Timetable';
import Id from '../utils/Id';

import styles from './styles.scss';

const ShowTable = ({ items }) => (<div>En desarrollo...</div>);

export default ShowTable;